{
  "name": "re-player-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright test project for tests authored by the replayer harness",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
